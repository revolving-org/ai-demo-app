app:
  name: ai-demo-app
  version: "0.1.0"
  debug: true

server:
  host: "0.0.0.0"
  port: 8000
  workers: 4

auth:
  token_expiry_seconds: 3600
  algorithm: "HS256"

rate_limit:
  requests_per_minute: 60
  burst_size: 10
